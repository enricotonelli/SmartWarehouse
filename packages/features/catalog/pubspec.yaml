name: catalog
description: "Catalog feature: product browsing, search and filtering."
version: 0.0.1
publish_to: none

environment:
  sdk: '>=3.8.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  commons:
    path: ../../commons
  core:
    path: ../../core
  design_system:
    path: ../../design_system
  bottom_navigation_bar:
    path: ../bottom_navigation_bar
  beamer: ^1.7.0
  dartz: ^0.10.1
  flutter_bloc: ^9.1.1
  google_fonts: ^6.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  dio: ^5.0.0

flutter:
